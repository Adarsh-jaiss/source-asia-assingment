version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      ENV: production
      APP_ENV: production
      PORT: ${PORT:-8080}
    ports:
      - "${API_PORT:-8080}:8080"
